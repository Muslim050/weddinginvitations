/**
 * Все обещания по срокам и условиям — в одном месте.
 *
 * Владелец должен подтвердить каждую строку: сайт не имеет права обещать то,
 * чего мастер не выполнит в сезон. Вычеркнуть лишнее проще здесь, чем искать
 * формулировки по десяти компонентам.
 */
export const promises = {
  /** Срок первого черновика после получения данных. */
  draft: "за один день",
  /** Что именно бесплатно до оплаты. */
  free: "Черновик с вашими именами — бесплатно",
  /** Условие оплаты. Самый сильный аргумент продавца. */
  payment: "Платите только если понравится",
  /** Сколько правок входит в стоимость. */
  edits: "две правки текста и фото",
  /** Сколько работает ссылка. */
  hosting: "год работы ссылки включён в стоимость",
  /** Что происходит в конце срока. */
  hostingEnd: "за месяц до конца напишу: продлеваем или отдаю архив",
} as const;
