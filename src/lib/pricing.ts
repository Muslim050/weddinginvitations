import { price } from "./site";

/**
 * Единственный источник правды по ценам.
 *
 * Тариф один: любой шаблон стоит одинаково, цена не зависит ни от числа
 * гостей, ни от набора опций. Каталог и блок цен читают одно и то же число,
 * чтобы страница не обещала в одном месте одно, а в другом другое.
 */
const PRICE = 1_400_000;

export const PRICES = {
  chinor: PRICE,
  nur: PRICE,
  nafis: PRICE,
  oud: PRICE,
  azure: PRICE,
  lumi: PRICE,
  garden: PRICE,
} as const;

/**
 * Уровень остался один — структура сохранена, чтобы вернуть градацию
 * можно было добавлением элемента, а не переписыванием блока цен.
 */
export type Tier = {
  id: string;
  value: number;
  /** Шаблоны, которые стоят этих денег. */
  templates: string[];
  description: string;
  /** Только отличия: общее для всех вынесено в `included`. */
  features: string[];
  highlight?: boolean;
};

/** Есть в любом шаблоне. Повторять это в каждой карточке — только шуметь. */
export const included = [
  "Русский и узбекский, гость переключает сам",
  "Анкета гостя: кто придёт и со сколькими",
  "Карта, маршрут и тайминг дня",
  "Год работы ссылки включён",
  "Две правки текста и фото",
];

export const tiers: Tier[] = [
  {
    id: "single",
    value: PRICE,
    highlight: true,
    templates: ["Chinor", "Nur", "Nafis", "Oud", "Azure", "Lumi", "Garden"],
    description:
      "Любой из семи шаблонов за одну цену. Выбираете по вкусу, а не по бюджету.",
    features: [
      "Светлая и тёмная тема на выбор гостя — в Chinor",
      "Видео-история и онлайн-трансляция — в Nur",
      "Свиток, конверт или сургучная печать в открытии — в авторских",
      "Фоновая музыка с управлением",
      "Программа вечера и добавление в календарь",
      "Книга пожеланий гостей",
    ],
  },
];

export { price };
